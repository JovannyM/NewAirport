<UserControl x:Class="NewAirport.VVM.Editer.ModelEditer.ModelEditerUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NewAirport.VVM.Editer.ModelEditer"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="1200"

             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Medium"
             TextElement.FontSize="14"
             FontFamily="{materialDesign:MaterialDesignFont}">

    <UserControl.DataContext>
        <local:ModelEditerVM />
    </UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <materialDesign:Card
            Grid.Row="0"
            materialDesign:ShadowAssist.ShadowDepth="Depth2"
            Margin="10,10,10,10"
            Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>


                <TextBlock
                    Grid.Row="0"
                    Margin="10 0 0 0"
                    Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                    Text="Новый шаблон"
                    FontWeight="Bold"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <StackPanel
                    Grid.Column="1"
                    Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
                    <TextBlock
                        Margin="10 0 0 0"
                        Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                        Text="Самолёт для шаблона"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right" />
                    <ComboBox
                        HorizontalAlignment="Left"
                        Margin="10 0 0 0"
                        MinWidth="250"
                        materialDesign:HintAssist.Hint="Search"
                        materialDesign:HintAssist.HintOpacity=".26"
                        IsEditable="True"
                        ItemsSource="{Binding ListOfAirplane}"
                        SelectedValue="{Binding CurrentTemplate.Airplane_Id}"
                        SelectedValuePath="Id"
                        DisplayMemberPath="Name" />
                </StackPanel>
                <Button
                    Grid.Column="4"
                    Margin="0 0 15 0"
                    Command="{Binding CreateFlight}"
                    Content="Создать" />
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card
            Grid.Row="1"
            materialDesign:ShadowAssist.ShadowDepth="Depth2"
            Margin="10,10,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <!-- Город -->
                <TextBlock
                    Grid.Row="0" Grid.Column="0"
                    Margin="10 10 10 10"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="СОЗДАНИЕ ПРИБЫТИЯ"
                    FontWeight="Bold"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <StackPanel
                    Grid.Row="0"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal">
                    <TextBlock
                        Margin="20 0 0 0"
                        Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                        Text="из аэропорта"
                        FontWeight="Bold"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center" />
                    <ComboBox
                        HorizontalAlignment="Left"
                        Margin="20 0 0 0"
                        MinWidth="250"
                        materialDesign:HintAssist.Hint="Search"
                        materialDesign:HintAssist.HintOpacity=".26"
                        IsEditable="True"
                        ItemsSource="{Binding ListOfAirports}"
                        SelectedValuePath="Id"
                        SelectedValue="{Binding CurrentTemplate.FirstAirport_Id }"
                        DisplayMemberPath="Name" />
                </StackPanel>

                <!-- День недели -->
                <TextBlock
                    Grid.Row="1" Grid.Column="0"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="День прибытия"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <ComboBox
                    Grid.Row="1" Grid.Column="1"
                    HorizontalAlignment="Left"
                    Margin="20 0 0 0"
                    MinWidth="200"
                    materialDesign:HintAssist.Hint="Search"
                    materialDesign:HintAssist.HintOpacity=".26"
                    IsEditable="True"
                    ItemsSource="{Binding DaysOfWeek}"
                    SelectedValuePath="Id"
                    SelectedValue="{Binding CurrentTemplate.ArrivalFromFirstCityDayOfWeek}"
                    DisplayMemberPath="Name" />

                <!-- Время -->
                <TextBlock
                    Grid.Row="2" Grid.Column="0"
                    Margin="10 0 0 0"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="Время отправления"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <materialDesign:TimePicker
                    Grid.Row="2"
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20 0 0 0"
                    Width="200"
                    SelectedTime="{Binding CurrentTemplate.DepartureTimeFromFirstCity}"
                    Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                    materialDesign:HintAssist.Hint="Время отправления" />

                <TextBlock
                    Grid.Row="2"
                    Grid.Column="2"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="Время прибытия"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <materialDesign:TimePicker
                    Grid.Row="2"
                    Grid.Column="3"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20 0 0 0"
                    Width="200"
                    SelectedTime="{Binding CurrentTemplate.ArrivalTimeFromFirstCity}"
                    Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                    materialDesign:HintAssist.Hint="Время прибытия" />
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card
            Grid.Row="2"
            Height="150"
            materialDesign:ShadowAssist.ShadowDepth="Depth2"
            Margin="10,10,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                    <RowDefinition Height="50" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <!-- Город -->
                <TextBlock
                    Grid.Row="0" Grid.Column="0"
                    Margin="10 0 0 0"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="СОЗДАНИЕ ОТПРАВЛЕНИЯ"
                    FontWeight="Bold"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <StackPanel
                    Grid.Row="0"
                    Grid.Column="1"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal">
                    <TextBlock
                        Margin="20 0 0 0"
                        Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                        Text="в аэропорт"
                        FontWeight="Bold"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center" />
                    <ComboBox
                        HorizontalAlignment="Left"
                        Margin="20 0 0 0"
                        MinWidth="250"
                        materialDesign:HintAssist.Hint="Search"
                        materialDesign:HintAssist.HintOpacity=".26"
                        IsEditable="True"
                        ItemsSource="{Binding ListOfAirports}"
                        SelectedValuePath="Id"
                        SelectedValue="{Binding CurrentTemplate.SecondAirport_Id }"
                        DisplayMemberPath="Name" />
                </StackPanel>


                <!-- День недели -->
                <TextBlock
                    Grid.Row="1" Grid.Column="0"
                    Margin="10 0 0 0"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="День отправления"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <ComboBox
                    Grid.Row="1" Grid.Column="1"
                    HorizontalAlignment="Left"
                    Margin="20 0 0 0"
                    MinWidth="200"
                    materialDesign:HintAssist.Hint="Search"
                    materialDesign:HintAssist.HintOpacity=".26"
                    IsEditable="True"
                    ItemsSource="{Binding DaysOfWeek}"
                    SelectedValuePath="Id"
                    SelectedValue="{Binding CurrentTemplate.DepartureToSecondCityDayOfWeek}"
                    DisplayMemberPath="Name" />

                <!-- Время -->
                <TextBlock
                    Grid.Row="2" Grid.Column="0"
                    Margin="10 0 0 0"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="Время отправления"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <materialDesign:TimePicker
                    Grid.Row="2"
                    Grid.Column="1"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20 0 0 0"
                    Width="200"
                    SelectedTime="{Binding CurrentTemplate.DepartureTimeToSecondCity}"
                    Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                    materialDesign:HintAssist.Hint="Время отправления" />

                <TextBlock
                    Grid.Row="2"
                    Grid.Column="2"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="Время прибытия"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <materialDesign:TimePicker
                    Grid.Row="2"
                    Grid.Column="3"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Margin="20 0 0 0"
                    Width="200"
                    SelectedTime="{Binding CurrentTemplate.ArrivalTimeToSecondCity}"
                    Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                    materialDesign:HintAssist.Hint="Время прибытия" />
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card
            Grid.Row="3"
            materialDesign:ShadowAssist.ShadowDepth="Depth2"
            Margin="10,10,10,10"
            Padding="0 10 0 10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>


                <TextBlock
                    Grid.Column="0"
                    Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                    Text="ПЕРИОД СОЗДАНИЯ"
                    FontWeight="Bold"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
                <StackPanel
                    Grid.Column="1"
                    Grid.ColumnSpan="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Left">
                    <DatePicker
                        Margin="20 0 0 0"
                        Width="150"
                        SelectedDate="{Binding CurrentTemplate.StartDateOfCreatingFlights}"
                        Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                        materialDesign:HintAssist.Hint="Дата начала"
                        SelectedDateFormat="Short" />
                    <materialDesign:TimePicker
                        Margin="20 0 0 0"
                        Width="150"
                        SelectedTime="{Binding CurrentTemplate.StartDateOfCreatingFlights}"
                        Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                        materialDesign:HintAssist.Hint="Время начала" />
                    <TextBlock
                        Margin="30 0 30 0"
                        Style="{StaticResource MaterialDesignSubtitle1TextBlock}"
                        Text="->"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right" />
                    <DatePicker
                        Width="150"
                        SelectedDate="{Binding CurrentTemplate.EndDateOfCreatingFlights}"
                        Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                        materialDesign:HintAssist.Hint="Дата окончания" />
                    <materialDesign:TimePicker
                        Margin="20 0 0 0"
                        Width="150"
                        SelectedTime="{Binding CurrentTemplate.EndDateOfCreatingFlights}"
                        Style="{StaticResource MaterialDesignFloatingHintTimePicker}"
                        materialDesign:HintAssist.Hint="Время окончания" />
                </StackPanel>
            </Grid>
        </materialDesign:Card>
    </Grid>


</UserControl>